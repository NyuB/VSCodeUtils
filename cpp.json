{
	"surround with if": {
		"body": [
			"if (${0}) {",
			"\t${TM_SELECTED_TEXT}",
			"}",
			"${1|else { }, |}",
		],
		"description": "Surround with if block"
	},
	"use as if condition": {
		"body": [
			"if (${TM_SELECTED_TEXT}) {",
			"\t${0}",
			"}",
			"${1|else { }, |}",
		],
		"description": "Use as condition of an if block"
	},
	"foreach": {
		"body": [
			"for (const auto& ${2:item}: ${1}) {",
			"\t${0}",
			"}",
		],
		"prefix": "foreach",
		"description": "Surround with foreach loop"
	},
	"disable clang format": {
		"body": [
			"// clang-format off",
			"${TM_SELECTED_TEXT}",
			"// clang-format on",
		],
		"description": "Disable clang-format for selection"
	},
}
